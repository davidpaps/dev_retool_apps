let data = table1.data.length //100
let page = table1.pagination.currentPage //0
let pageSize = table1.pagination.pageSize //10
let pages = data / pageSize
let result = []

for(let i = 0; i < pageSize; i++){

  if(){
    result.push()
  }
}

return result
